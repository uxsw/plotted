{
  "schemaVersion": 2,
  "generatedAt": "2026-09-20T19:44:25Z",
  "title": "Design System: Plotted",
  "extensions": {
    "colorMeta": {
      "marigold": {
        "role": "primary",
        "displayName": "Marigold",
        "canonical": "oklch(65.809% 0.15943 31.855)",
        "tonalRamp": [
          "oklch(20% 0.080 31.9)",
          "oklch(30% 0.110 31.9)",
          "oklch(40% 0.140 31.9)",
          "oklch(50% 0.160 31.9)",
          "oklch(58% 0.165 31.9)",
          "oklch(65.8% 0.159 31.9)",
          "oklch(78% 0.120 31.9)",
          "oklch(91% 0.050 31.9)"
        ]
      },
      "highlight-yellow": {
        "role": "secondary",
        "displayName": "Highlight Yellow",
        "canonical": "oklch(94.577% 0.10215 110.6)",
        "tonalRamp": [
          "oklch(35% 0.060 110.6)",
          "oklch(48% 0.080 110.6)",
          "oklch(60% 0.100 110.6)",
          "oklch(72% 0.110 110.6)",
          "oklch(82% 0.110 110.6)",
          "oklch(89% 0.108 110.6)",
          "oklch(94.6% 0.102 110.6)",
          "oklch(97.5% 0.050 110.6)"
        ]
      },
      "lavender": {
        "role": "tertiary",
        "displayName": "Scheme Lavender",
        "canonical": "oklch(52.839% 0.09034 282.87)",
        "tonalRamp": [
          "oklch(20% 0.060 282.9)",
          "oklch(30% 0.080 282.9)",
          "oklch(40% 0.090 282.9)",
          "oklch(52.8% 0.090 282.9)",
          "oklch(64% 0.090 282.9)",
          "oklch(76% 0.080 282.9)",
          "oklch(87% 0.050 282.9)",
          "oklch(96% 0.030 282.9)"
        ]
      },
      "cyan": {
        "role": "tertiary",
        "displayName": "Focus Cyan",
        "canonical": "oklch(78.91% 0.07815 206.15)",
        "tonalRamp": [
          "oklch(25% 0.050 206.2)",
          "oklch(37% 0.065 206.2)",
          "oklch(48% 0.075 206.2)",
          "oklch(60% 0.080 206.2)",
          "oklch(70% 0.080 206.2)",
          "oklch(78.9% 0.078 206.2)",
          "oklch(88% 0.050 206.2)",
          "oklch(96% 0.025 206.2)"
        ]
      },
      "paper": {
        "role": "neutral",
        "displayName": "Warm Paper",
        "canonical": "#FAF6EC",
        "tonalRamp": [
          "oklch(20% 0.010 95)",
          "oklch(32% 0.012 95)",
          "oklch(45% 0.013 95)",
          "oklch(58% 0.014 95)",
          "oklch(72% 0.015 95)",
          "oklch(84% 0.015 95)",
          "oklch(92% 0.014 95)",
          "oklch(97.2% 0.012 95)"
        ]
      },
      "sand": {
        "role": "neutral",
        "displayName": "Sand Panel",
        "canonical": "#E8DFC8",
        "tonalRamp": [
          "oklch(22% 0.015 92)",
          "oklch(34% 0.020 92)",
          "oklch(47% 0.025 92)",
          "oklch(60% 0.028 92)",
          "oklch(73% 0.030 92)",
          "oklch(83% 0.030 92)",
          "oklch(90% 0.028 92)",
          "oklch(95% 0.020 92)"
        ]
      },
      "ink": {
        "role": "neutral",
        "displayName": "Ink",
        "canonical": "#2B2A24",
        "tonalRamp": [
          "oklch(15% 0.004 95)",
          "oklch(22% 0.005 95)",
          "oklch(26% 0.006 95)",
          "oklch(38% 0.007 95)",
          "oklch(50% 0.008 95)",
          "oklch(64% 0.008 95)",
          "oklch(80% 0.006 95)",
          "oklch(93% 0.004 95)"
        ]
      },
      "n-deep-grey": {
        "role": "neutral",
        "displayName": "Deep Grey (body text)",
        "canonical": "oklch(29.716% 0 0)",
        "tonalRamp": [
          "oklch(15% 0 0)",
          "oklch(22% 0 0)",
          "oklch(29.7% 0 0)",
          "oklch(40% 0 0)",
          "oklch(52% 0 0)",
          "oklch(65% 0 0)",
          "oklch(80% 0 0)",
          "oklch(93% 0 0)"
        ]
      }
    },
    "typographyMeta": {
      "display": {
        "displayName": "Display",
        "purpose": "The big statement headline. Italic 400 on the marketing hero; roman 600 for the equivalent in-app heading. Fluid clamp on the .canon step."
      },
      "headline": {
        "displayName": "Headline",
        "purpose": "App page titles, section headers, empty-state and dialog titles. Fraunces bold roman 600 (.kirk), .paragon step. Marketing section headings take italic 400 instead."
      },
      "title": {
        "displayName": "Title",
        "purpose": "Card titles and sub-section headers. Fraunces bold roman 600 (.kirk), .pica / .long-primer step. Botanical Latin inside is additionally italicised."
      },
      "editorial-display-body": {
        "displayName": "Editorial display-body",
        "purpose": "Long narrative-intro paragraphs set in Fraunces roman 400 (no .kirk), .long-primer / .primer size — e.g. a scheme's narrative_intro. Not a heading; the absence of .kirk is what separates it."
      },
      "body": {
        "displayName": "Body",
        "purpose": "All running text. Inter 400, .primer step, line-height 1.5. Capped near 65ch with .o-measure. Colour is n-deep-grey, not ink."
      },
      "body-sm": {
        "displayName": "Body-sm",
        "purpose": "Secondary text, card subtitles, helper and hint copy. Inter 400, .brevier step."
      },
      "caption": {
        "displayName": "Caption / data",
        "purpose": "Metadata, timestamps, badge text; dense numeral columns with .o-type-tabular. Inter, .minion step."
      },
      "label": {
        "displayName": "Mono Label",
        "purpose": "Eyebrows, plate numbers, ALL field labels, status pills, botanical metadata. One class: .o-type-label (Spline Sans Mono, .minion, weight 500, uppercase, 0.14em). The earlier Inter small-caps variant on form fields is retired."
      }
    },
    "shadows": [
      {
        "name": "menu-raised",
        "value": "0 4px 6px -1px #0000001a, 0 2px 4px -2px #0000001a",
        "purpose": "Popovers and the autocomplete menu. The one consolidated low step."
      },
      {
        "name": "card-hover",
        "value": "0 8px 24px rgba(0,0,0,0.15)",
        "purpose": "Lift on interactive cards on hover (also z-index:1) and on scheme frame panels. The raised step."
      },
      {
        "name": "dialog",
        "value": "0 20px 25px -5px #0000001a, 0 8px 10px -6px #0000001a",
        "purpose": "Modal dialogs only. The deepest step, for content that stops the page."
      },
      {
        "name": "focus-ring-lavender",
        "value": "0 0 0 2px var(--color-paper), 0 0 0 4px var(--color-p-lavender)",
        "purpose": "Button-component focus-visible ring, floated off the paper ground. Fields and menus instead use a 2px cyan outline."
      }
    ],
    "motion": [
      {
        "name": "transition-default",
        "value": "0.4s ease",
        "purpose": "Token default (--transition-time-default). Colour/padding transitions on objects."
      },
      {
        "name": "transition-slow",
        "value": "0.8s ease",
        "purpose": "--transition-time-slow, for larger reveals."
      },
      {
        "name": "transition-emphasis",
        "value": "cubic-bezier(0.16, 1, 0.3, 1)",
        "purpose": "--transition-easing-emphasis. The one authored moment on a surface: the scheme-entry plate lift + engraving bloom, the scheme-scratch schedule row easing in as it is added. Exponential ease-out from an already-visible resting state; never for incidental hovers. Degrades to an instant state change under prefers-reduced-motion."
      },
      {
        "name": "chat-typing",
        "value": "opacity 0.3->1 + a 0.125rem rise, 1.2s ease, dots staggered 0.15s, infinite",
        "purpose": "_chat.scss keyframe — the typing indicator's three dots while the assistant composes. The chat surface's one authored moment; static (opacity 0.55) under prefers-reduced-motion."
      },
      {
        "name": "the-planting",
        "value": "scheme-item-in (fade + -0.25rem settle) + scheme-card-wash (--_accent-wash -> white, 0.8s) + scheme-elevation-draw/-dot (stroke-dashoffset 1 -> 0, pathLength-normalised; dots bud a beat after their stem), all on the emphasis curve with a shared per-item --_delay",
        "purpose": "_scheme-chat.scss keyframe family — the scheme workspace's one authored moment: one orchestrated response to a plant joining the scheme (its list card, its chat suggestion card, and its silhouette drawing up from the elevation's ground line). --_delay is 0 for a single add and staggered ~70–90ms per item when a populated sheet or fresh suggestion panel first arrives, so a scheme lands as a planting, not a dump. Every keyframe is animation: none under prefers-reduced-motion."
      },
      {
        "name": "micro-150",
        "value": "150ms",
        "purpose": "Hardcoded in Button and .o-card--interactive for tight hover/press feedback. A known divergence from the 0.4s token."
      },
      {
        "name": "press-75",
        "value": "75ms",
        "purpose": "transform/opacity on :active press (card scale 0.98, opacity 0.75)."
      },
      {
        "name": "reduced-motion",
        "value": "@media (prefers-reduced-motion: reduce) { animation: none }",
        "purpose": "Every keyframe animation opts out here; spatial entrance/hover motion is also gated behind (prefers-reduced-motion: no-preference)."
      }
    ],
    "breakpoints": [
      {
        "name": "mobile",
        "value": "480px"
      },
      {
        "name": "tablet",
        "value": "860px"
      }
    ]
  },
  "components": [
    {
      "name": "Primary Button",
      "kind": "button",
      "refersTo": "button-primary",
      "description": "The single primary action per view. Marigold fill, warm off-white text, near-square corners.",
      "html": "<button class=\"ds-btn-primary\">Add plant</button>",
      "css": ".ds-btn-primary { display: inline-flex; align-items: center; justify-content: center; gap: 0.5rem; padding: 0.75rem 1rem; border: none; border-radius: var(--radius-s, 2px); font-family: var(--font-inter), sans-serif; font-size: 0.875rem; line-height: 1; white-space: nowrap; inline-size: max-content; cursor: pointer; background-color: var(--color-r-marigold); color: var(--color-ink-deep); transition: background-color 150ms, color 150ms, border-color 150ms; } .ds-btn-primary:hover { background-color: var(--color-o-vermillion); color: var(--color-ink-deep); } .ds-btn-primary:focus-visible { outline: none; box-shadow: 0 0 0 2px var(--color-paper), 0 0 0 4px var(--color-p-lavender); } .ds-btn-primary:disabled { opacity: 0.5; cursor: not-allowed; }"
    },
    {
      "name": "Secondary Button",
      "kind": "button",
      "refersTo": "button-secondary",
      "description": "Neutral-weight action. Transparent with a thin near-black border; hover fills highlight-yellow.",
      "html": "<button class=\"ds-btn-secondary\">Edit</button>",
      "css": ".ds-btn-secondary { display: inline-flex; align-items: center; justify-content: center; gap: 0.5rem; padding: 0.75rem 1rem; border-radius: var(--radius-s, 2px); font-family: var(--font-inter), sans-serif; font-size: 0.875rem; line-height: 1; white-space: nowrap; inline-size: max-content; cursor: pointer; background-color: transparent; color: var(--color-n-deep-grey); border: 1px solid var(--color-n-deep-grey); transition: background-color 150ms, color 150ms, border-color 150ms; } .ds-btn-secondary:hover { background-color: var(--color-y-yellow); border-color: var(--color-y-yellow); } .ds-btn-secondary:focus-visible { outline: none; box-shadow: 0 0 0 2px var(--color-paper), 0 0 0 4px var(--color-p-lavender); } .ds-btn-secondary:disabled { opacity: 0.5; cursor: not-allowed; }"
    },
    {
      "name": "Ghost Button",
      "kind": "button",
      "refersTo": "button-ghost",
      "description": "Lowest-stakes inline action. No border; hover fills highlight-yellow.",
      "html": "<button class=\"ds-btn-ghost\">Cancel</button>",
      "css": ".ds-btn-ghost { display: inline-flex; align-items: center; justify-content: center; gap: 0.5rem; padding: 0.75rem 1rem; border: none; border-radius: var(--radius-s, 2px); font-family: var(--font-inter), sans-serif; font-size: 0.875rem; line-height: 1; white-space: nowrap; inline-size: max-content; cursor: pointer; background-color: transparent; color: var(--color-n-deep-grey); transition: background-color 150ms, color 150ms; } .ds-btn-ghost:hover { background-color: var(--color-y-yellow); } .ds-btn-ghost:focus-visible { outline: none; box-shadow: 0 0 0 2px var(--color-paper), 0 0 0 4px var(--color-p-lavender); }"
    },
    {
      "name": "Scheme Button",
      "kind": "button",
      "refersTo": "button-scheme",
      "description": "The Planting Schemes context's own action colour — lavender fill, lavender-white text. Reserved for a scheme's own finishing actions (e.g. \"Generate the scheme\") so it never competes with marigold, which stays the one primary accent elsewhere on the same view (e.g. the chat composer's send button).",
      "html": "<button class=\"ds-btn-scheme\"><svg width=\"16\" height=\"16\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" aria-hidden=\"true\"><path d=\"M14 9.536V7a4 4 0 0 1 4-4h1.5a.5.5 0 0 1 .5.5V5a4 4 0 0 1-4 4 4 4 0 0 0-4 4c0 2 1 3 1 5a5 5 0 0 1-1 3\"/><path d=\"M4 9a5 5 0 0 1 8 4 5 5 0 0 1-8-4\"/><path d=\"M5 21h14\"/></svg>Generate the scheme</button>",
      "css": ".ds-btn-scheme { display: inline-flex; align-items: center; justify-content: center; gap: 0.5rem; padding: 0.75rem 1rem; border: none; border-radius: var(--radius-s, 2px); font-family: var(--font-inter), sans-serif; font-size: 0.875rem; line-height: 1; white-space: nowrap; inline-size: max-content; cursor: pointer; background-color: var(--color-p-lavender); color: var(--color-p-white); transition: background-color 150ms, color 150ms; } .ds-btn-scheme:hover { background-color: var(--color-o-vermillion); color: var(--color-n-deep-grey); } .ds-btn-scheme:focus-visible { outline: none; box-shadow: 0 0 0 2px var(--color-paper), 0 0 0 4px var(--color-p-lavender); } .ds-btn-scheme:disabled { opacity: 0.5; cursor: not-allowed; }"
    },
    {
      "name": "Text Input",
      "kind": "input",
      "refersTo": "input",
      "description": "Standard field. Mono label above (.o-type-label — Spline Sans Mono, uppercase, 0.14em); paper fill, sand-line border, strong marigold focus (border, ring and fill all shift).",
      "html": "<div class=\"ds-field\"><label class=\"ds-field__label\" for=\"ds-species\">Species</label><input class=\"ds-field__input\" id=\"ds-species\" type=\"text\" placeholder=\"e.g. canina\" /></div>",
      "css": ".ds-field { display: flex; flex-direction: column; gap: 0.375rem; } .ds-field__label { font-family: var(--font-spline-mono), ui-monospace, monospace; font-size: 0.75rem; font-weight: 500; text-transform: uppercase; letter-spacing: 0.14em; line-height: 1.4; color: var(--color-ink-soft); } .ds-field__input { border: 1px solid var(--color-sand-line); border-radius: var(--radius-m, 4px); padding: 0.5rem 0.75rem; font-family: var(--font-inter), sans-serif; font-size: 0.875rem; color: var(--color-ink); background-color: var(--color-paper); transition: background-color 150ms, border-color 150ms, box-shadow 150ms; } .ds-field__input::placeholder { color: color-mix(in srgb, var(--color-ink-soft) 50%, transparent); } .ds-field__input:focus { outline: none; border-color: var(--color-r-marigold); background-color: var(--color-r-marigold); box-shadow: 0 0 0 1px var(--color-paper), 0 0 0 3px var(--color-r-marigold); }"
    },
    {
      "name": "Toggle",
      "kind": "input",
      "refersTo": null,
      "description": "A 44×24 pill switch (role=switch) with a 16px knob. Off is an outlined track (transparent fill, n-dark-grey border and knob); on is a filled track (cyan-deep) with a white knob — the two states differ by fill and knob position, never colour alone.",
      "html": "<div class=\"ds-toggle-row\"><button class=\"ds-toggle\" role=\"switch\" aria-checked=\"false\" aria-label=\"Off\"><span class=\"ds-toggle__knob\"></span></button><button class=\"ds-toggle ds-toggle--on\" role=\"switch\" aria-checked=\"true\" aria-label=\"On\"><span class=\"ds-toggle__knob\"></span></button></div>",
      "css": ".ds-toggle-row { display: flex; gap: 12px; align-items: center; } .ds-toggle { position: relative; display: inline-flex; align-items: center; inline-size: 44px; block-size: 24px; padding: 0; border: 1px solid var(--color-n-dark-grey); border-radius: var(--radius-pill, 32px); background-color: transparent; cursor: pointer; transition: background-color 0.4s ease, border-color 0.4s ease; } .ds-toggle__knob { position: absolute; inset-inline-start: 5px; inline-size: 16px; block-size: 16px; border-radius: 50%; background-color: var(--color-n-dark-grey); transition: transform 0.4s ease, background-color 0.4s ease; } .ds-toggle--on { background-color: var(--color-b-cyan-deep); border-color: var(--color-b-cyan-deep); } .ds-toggle--on .ds-toggle__knob { background-color: var(--color-white); transform: translateX(20px); }"
    },
    {
      "name": "Photo Dropzone",
      "kind": "input",
      "refersTo": null,
      "description": "The add-a-plant photo well (`.c-add-photo`). A 172px band on the warm orange fill with a dashed marigold edge held in both rest and hover (the fill alone is too pale to define the zone); hover swaps the fill to highlight-yellow and softens the corner.",
      "html": "<button type=\"button\" class=\"ds-dropzone\" aria-label=\"Add a photo\"><svg width=\"24\" height=\"24\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" aria-hidden=\"true\"><path d=\"M13.997 4a2 2 0 0 1 1.76 1.05l.486.9A2 2 0 0 0 18.003 7H20a2 2 0 0 1 2 2v9a2 2 0 0 1-2 2H4a2 2 0 0 1-2-2V9a2 2 0 0 1 2-2h1.997a2 2 0 0 0 1.759-1.048l.489-.904A2 2 0 0 1 10.004 4z\"/><circle cx=\"12\" cy=\"13\" r=\"3\"/></svg><span>Add a photo</span></button>",
      "css": ".ds-dropzone { display: flex; flex-direction: column; align-items: center; justify-content: center; gap: 0.5rem; inline-size: 100%; max-inline-size: 320px; block-size: 172px; border: 2px dashed var(--color-r-marigold); border-radius: var(--radius-s, 2px); background-color: var(--color-o-orange); color: var(--color-ink-soft); font-family: var(--font-mono), ui-monospace, monospace; font-size: 0.73rem; letter-spacing: 0.14em; text-transform: uppercase; cursor: pointer; transition: background-color 0.4s ease, color 0.4s ease, border-radius 0.4s ease; } .ds-dropzone:hover { background-color: var(--color-y-yellow); color: var(--color-n-deep-grey); border-radius: var(--radius-m, 4px); }"
    },
    {
      "name": "Radio Card",
      "kind": "input",
      "refersTo": null,
      "description": "A photo card acting as one option in a picker (`.c-identify-option` / the garden-gallery multi-select tile). Selection can't be a fill on a photograph, so it's carried by a mark that is always drawn: an empty ring unchosen, a filled cyan-deep disc with a white check when chosen. Hover is the card lift only — the edge never borrows the selected treatment.",
      "html": "<div class=\"ds-radio-row\"><div class=\"ds-radio-card\"><div class=\"ds-radio-card__media\"><span class=\"ds-radio-card__mark\"></span></div></div><div class=\"ds-radio-card ds-radio-card--chosen\"><div class=\"ds-radio-card__media\"><span class=\"ds-radio-card__mark\"><svg width=\"10\" height=\"10\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"3\" stroke-linecap=\"round\" stroke-linejoin=\"round\" aria-hidden=\"true\"><path d=\"M20 6 9 17l-5-5\"/></svg></span></div></div></div>",
      "css": ".ds-radio-row { display: flex; gap: 16px; } .ds-radio-card { position: relative; inline-size: 96px; block-size: 96px; border-radius: var(--radius-m, 4px); border: 1px solid var(--color-n-dark-grey); background-color: var(--color-y-yellow); overflow: hidden; transition: box-shadow 150ms; } .ds-radio-card:hover { box-shadow: 0 8px 24px rgba(0,0,0,0.15); } .ds-radio-card--chosen { border-color: var(--color-b-cyan-deep); box-shadow: 0 0 0 1px var(--color-b-cyan-deep); } .ds-radio-card__media { inline-size: 100%; block-size: 100%; } .ds-radio-card__mark { position: absolute; inset-block-start: 8px; inset-inline-end: 8px; display: inline-flex; align-items: center; justify-content: center; inline-size: 18px; block-size: 18px; border-radius: 50%; background-color: var(--color-white); border: 2px solid var(--color-n-dark-grey); box-shadow: 0 0 0 1px var(--color-white); color: transparent; } .ds-radio-card--chosen .ds-radio-card__mark { background-color: var(--color-b-cyan-deep); border: 2px solid var(--color-white); color: var(--color-white); }"
    },
    {
      "name": "Trait Badge",
      "kind": "chip",
      "refersTo": "badge",
      "description": "Static, non-interactive label. Pill, 1px border, colour set per trait via the --sem-* semantic layer. Shown here in the 'edible' variant.",
      "html": "<span class=\"ds-badge ds-badge--edible\">Edible</span>",
      "css": ".ds-badge { --_bg: var(--color-paper); --_fg: var(--color-ink-soft); --_bd: var(--sem-border-color); display: inline-flex; align-items: center; justify-content: center; gap: var(--space-xs, 4px); border-radius: var(--radius-pill, 32px); border: 1px solid var(--_bd); background-color: var(--_bg); color: var(--_fg); font-family: var(--font-inter), sans-serif; font-size: 0.85rem; line-height: 2; padding-inline: var(--space-sm, 8px); white-space: nowrap; width: max-content; } .ds-badge--edible { --_bg: var(--sem-edible-bg); --_fg: var(--sem-edible-fg); --_bd: var(--sem-edible-bg); } .ds-badge--sm { font-size: 0.65rem; line-height: 1.5; padding-inline: var(--space-xs, 4px); }"
    },
    {
      "name": "Chip",
      "kind": "chip",
      "refersTo": "chip",
      "description": "Interactive tag (filter / dismissible). Pill, asymmetric padding to seat a trailing circular action; hover warms to gold.",
      "html": "<span class=\"ds-chip\">Foxglove<button class=\"ds-chip__action\" aria-label=\"Remove\"><svg width=\"12\" height=\"12\" viewBox=\"0 0 12 12\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"1.5\" stroke-linecap=\"round\"><path d=\"M3 3l6 6M9 3l-6 6\"/></svg></button></span>",
      "css": ".ds-chip { display: inline-flex; align-items: center; justify-content: center; gap: var(--space-xs, 4px); border-radius: var(--radius-pill, 32px); font-family: var(--font-inter), sans-serif; font-size: 0.85rem; line-height: 2; padding-inline-start: var(--space-sm, 8px); padding-inline-end: var(--space-xs, 4px); white-space: nowrap; width: max-content; background-color: transparent; color: var(--color-ink-soft); transition: background-color 0.4s ease; } .ds-chip:hover { background-color: var(--color-y-gold); } .ds-chip__action { display: flex; align-items: center; justify-content: center; height: 24px; width: 24px; padding: 0; border: none; border-radius: 50%; background-color: transparent; color: inherit; cursor: pointer; transition: background-color 0.4s ease; } .ds-chip__action:hover { background-color: var(--color-white); }"
    },
    {
      "name": "Plant Card",
      "kind": "card",
      "refersTo": "card",
      "description": "The core content unit. White raised surface on grained paper, 4px corners, flat at rest, lifts on hover; 4:3 media well with badge slot. Bold-roman Fraunces title with the botanical binomial italicised (a pure-Latin title reads fully italic).",
      "html": "<a class=\"ds-card\" href=\"#\"><div class=\"ds-card__media\"><span class=\"ds-card__badge\">Full sun</span></div><div class=\"ds-card__body\"><h3 class=\"ds-card__title\">Digitalis purpurea</h3><p class=\"ds-card__sub\">Foxglove</p></div></a>",
      "css": ".ds-card { display: flex; flex-direction: column; height: 100%; overflow: hidden; border-radius: var(--radius-m, 4px); background-color: var(--color-white); text-decoration: none; color: inherit; transition: box-shadow 150ms, transform 75ms, opacity 75ms; } .ds-card:hover { box-shadow: 0 8px 24px rgba(0,0,0,0.15); z-index: 1; } .ds-card:active { transform: scale(0.98); opacity: 0.75; } .ds-card__media { position: relative; width: 100%; aspect-ratio: 4 / 3; background-color: var(--color-y-yellow); } .ds-card__badge { position: absolute; top: var(--space-xs, 4px); right: var(--space-xs, 4px); display: inline-flex; align-items: center; border-radius: var(--radius-pill, 32px); border: 1px solid var(--sem-full-sun-bg); background-color: var(--sem-full-sun-bg); color: var(--sem-full-sun-fg); font-family: var(--font-inter), sans-serif; font-size: 0.65rem; line-height: 1.5; padding-inline: var(--space-xs, 4px); } .ds-card__body { display: flex; flex-direction: column; gap: 0.375rem; padding: var(--space-md, 16px); flex: 1; } .ds-card__title { margin: 0; font-family: var(--font-fraunces), serif; font-style: italic; font-weight: 600; font-size: 1.05rem; line-height: 1.2; color: var(--color-ink); } .ds-card__sub { margin: 0; font-family: var(--font-inter), sans-serif; font-size: 0.85rem; color: var(--color-ink-soft); }"
    },
    {
      "name": "Primary Nav",
      "kind": "nav",
      "refersTo": "nav-item",
      "description": "Equal-width tab strip under the app header. Transparent 2px bottom border; hover shows a marigold underline, active adds marigold text and medium weight.",
      "html": "<nav class=\"ds-nav\" aria-label=\"Primary\"><a class=\"ds-nav__item ds-nav__item--active\" href=\"#\" aria-current=\"page\">Home</a><a class=\"ds-nav__item\" href=\"#\">Plants</a><a class=\"ds-nav__item\" href=\"#\">Schemes</a></nav>",
      "css": ".ds-nav { display: flex; border-bottom: 1px solid var(--sem-border-color); } .ds-nav__item { flex: 1; display: flex; justify-content: center; padding-top: var(--space-sm, 8px); margin-bottom: -1px; font-family: var(--font-inter), sans-serif; font-size: 0.9rem; text-decoration: none; color: var(--color-n-deep-grey); border-bottom: 2px solid transparent; transition: color 0.4s ease, border-bottom-color 0.4s ease; } .ds-nav__item:hover { border-bottom-color: var(--color-r-marigold); } .ds-nav__item--active { color: var(--color-r-marigold); border-bottom-color: var(--color-r-marigold); font-weight: 500; }"
    },
    {
      "name": "Popover Menu",
      "kind": "custom",
      "refersTo": null,
      "description": "The single shared dropdown shell (user menu, scheme actions, plant filter). White, hairline border, 4px radius, low menu shadow; items fill highlight-yellow on hover, danger items fill smoke-red.",
      "html": "<div class=\"ds-popover\"><button class=\"ds-popover__item\">Rename scheme</button><button class=\"ds-popover__item\">Duplicate</button><button class=\"ds-popover__item ds-popover__item--danger\">Delete scheme</button></div>",
      "css": ".ds-popover { display: flex; flex-direction: column; gap: var(--space-sm, 8px); min-width: 200px; padding: var(--space-xs, 4px); background-color: var(--color-white); border: 1px solid var(--sem-border-color); border-radius: var(--radius-m, 4px); box-shadow: 0 4px 6px -1px #0000001a, 0 2px 4px -2px #0000001a; } .ds-popover__item { display: flex; align-items: center; gap: var(--space-sm, 8px); width: 100%; padding: var(--space-sm, 8px) var(--space-md, 16px); border: none; background-color: transparent; color: var(--color-n-deep-grey); font-family: var(--font-inter), sans-serif; font-size: 0.9rem; text-align: left; cursor: pointer; transition: background-color 0.4s ease; } .ds-popover__item:hover { background-color: var(--color-y-yellow); } .ds-popover__item:focus-visible { outline: 2px solid var(--color-b-cyan); } .ds-popover__item--danger:hover { background-color: var(--color-r-smoke-red); }"
    },
    {
      "name": "Chat Bubble",
      "kind": "custom",
      "refersTo": "card",
      "description": "One turn in the c-chat conversation surface. Assistant bubbles on paper-deep (borderless), the person's on white with a hairline; each squares one corner toward its speaker. The 'Plotted' attribution with a sprout mark shows once at the head of an assistant run. Role is alignment + fill tone + attribution, never colour alone.",
      "html": "<div class=\"ds-chat\">\n  <div class=\"ds-chat__msg ds-chat__msg--assistant\">\n    <span class=\"ds-chat__from\"><svg class=\"ds-chat__avatar\" viewBox=\"0 0 20 20\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"1.4\" stroke-linecap=\"round\" stroke-linejoin=\"round\" aria-hidden=\"true\"><path d=\"M10 18V8\"/><path d=\"M10 11.5C6.4 11 3.8 8.4 3.3 4.8 6.9 5.3 9.5 7.9 10 11.5Z\"/><path d=\"M10 9.5C13 9 15 6.6 15.4 3.6 12.4 4 10.4 6 10 9.5Z\"/></svg>Plotted</span>\n    <div class=\"ds-chat__bubble\">I'll help you turn this into a full planting scheme &mdash; ask for a swap, more options in a direction, or tell me what isn't working.</div>\n  </div>\n  <div class=\"ds-chat__msg ds-chat__msg--user\">\n    <div class=\"ds-chat__bubble\">Full sun, mostly.</div>\n  </div>\n</div>",
      "css": ".ds-chat { display: flex; flex-direction: column; gap: 16px; }\n.ds-chat__msg { display: flex; flex-direction: column; gap: 4px; max-inline-size: min(100%, 42rem); }\n.ds-chat__msg--assistant { align-self: flex-start; align-items: flex-start; }\n.ds-chat__msg--user { align-self: flex-end; align-items: flex-end; }\n.ds-chat__from { display: inline-flex; align-items: center; gap: 4px; font-family: var(--font-mono); font-size: 0.8rem; font-weight: 500; letter-spacing: 0.14em; text-transform: uppercase; color: var(--color-ink-soft); }\n.ds-chat__avatar { flex: none; inline-size: 1.25rem; block-size: 1.25rem; color: var(--color-r-marigold); }\n.ds-chat__bubble { padding: 8px 16px; line-height: 1.55; font-size: clamp(0.909rem, 0.102vw + 0.891rem, 1rem); border-radius: 8px; overflow-wrap: break-word; }\n.ds-chat__msg--assistant .ds-chat__bubble { color: var(--color-n-deep-grey); background: var(--color-paper-deep); border-end-start-radius: 2px; }\n.ds-chat__msg--user .ds-chat__bubble { color: var(--color-n-deep-grey); background: var(--color-white); border: 1px solid var(--sem-border-color); border-end-end-radius: 2px; }"
    },
    {
      "name": "Typing Indicator",
      "kind": "custom",
      "refersTo": "card",
      "description": "Shown in the c-chat log while the assistant composes a reply. Three dots in a paper-deep bubble; role=status with a visually-hidden 'Plotted is thinking'. The chat surface's one authored moment.",
      "html": "<div class=\"ds-typing\" role=\"status\" aria-label=\"Plotted is thinking\"><span></span><span></span><span></span></div>",
      "css": ".ds-typing { align-self: flex-start; display: inline-flex; gap: 0.25rem; align-items: center; padding: 8px 16px; background: var(--color-paper-deep); border-radius: 8px; border-end-start-radius: 2px; }\n.ds-typing span { inline-size: 0.375rem; block-size: 0.375rem; border-radius: 50%; background: var(--color-ink-soft); animation: ds-typing 1.2s ease infinite; }\n.ds-typing span:nth-child(2) { animation-delay: 0.15s; }\n.ds-typing span:nth-child(3) { animation-delay: 0.3s; }\n@keyframes ds-typing { 0%, 60%, 100% { opacity: 0.3; transform: translateY(0); } 30% { opacity: 1; transform: translateY(-0.125rem); } }\n@media (prefers-reduced-motion: reduce) { .ds-typing span { animation: none; opacity: 0.55; } }"
    },
    {
      "name": "Chat Composer",
      "kind": "input",
      "refersTo": "input",
      "description": "The c-chat message input: an auto-growing textarea (field-sizing: content, repeated-entry focus — marigold ring + border, no fill flip, marigold caret) and a 44px roundel send button in the brand accent. Enter sends, Shift+Enter newlines.",
      "html": "<div class=\"ds-composer\">\n  <textarea class=\"ds-composer__input\" rows=\"1\" placeholder=\"Ask for a swap, more options, a different direction&hellip;\" aria-label=\"Message Plotted\"></textarea>\n  <button class=\"ds-composer__send\" aria-label=\"Send message\"><svg width=\"16\" height=\"16\" viewBox=\"0 0 16 16\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"1.75\" stroke-linecap=\"round\" stroke-linejoin=\"round\" aria-hidden=\"true\"><path d=\"M8 13.5V3\"/><path d=\"M3.5 7.5 8 3l4.5 4.5\"/></svg></button>\n</div>",
      "css": ".ds-composer { display: flex; align-items: flex-end; gap: 8px; }\n.ds-composer__input { flex: 1; min-inline-size: 0; resize: none; padding: 8px 16px; font: inherit; font-size: clamp(0.813rem, 0.092vw + 0.797rem, 0.894rem); line-height: 1.5; color: var(--color-ink); background: var(--color-paper); border: 1px solid var(--color-sand-line); border-radius: 8px; caret-color: var(--color-r-marigold); field-sizing: content; max-block-size: 8rem; overflow-y: auto; }\n.ds-composer__input::placeholder { color: color-mix(in srgb, var(--color-ink-soft) 60%, transparent); }\n.ds-composer__input:focus-visible { outline: none; border-color: var(--color-r-marigold); box-shadow: 0 0 0 2px var(--color-r-marigold); }\n.ds-composer__send { flex: none; display: inline-flex; align-items: center; justify-content: center; inline-size: 2.75rem; block-size: 2.75rem; color: var(--color-r-white); background: var(--color-r-marigold); border: none; border-radius: 50%; cursor: pointer; transition: background-color 0.4s ease; }\n.ds-composer__send:hover { background: var(--color-o-vermillion); }\n.ds-composer__send:disabled { opacity: 0.4; cursor: not-allowed; }\n.ds-composer__send:focus-visible { outline: none; box-shadow: 0 0 0 2px var(--color-paper), 0 0 0 4px var(--color-p-lavender); }"
    },
    {
      "name": "Living Border Sheet",
      "kind": "custom",
      "refersTo": "card",
      "description": "The head of the /plant-scheme/chat scheme list — the list drawn as a border taking shape, not a list. Under the season-washed letterhead: the border elevation (an engraved cross-section on a shared ground line — every tiered plant one silhouette in the path accent, a dashed ghost holding any empty tier) and the flowering year (twelve month cells filling with their season's --sem-flowering-* pair, a small accent tick under the current month). The elevation and year are held to a centred ~22rem figure measure so they stay diagram-scale on a wider sheet. New silhouettes draw themselves in on the emphasis curve — part of 'the planting' (see motion).",
      "html": "<div class=\"ds-sheet\">\n  <div class=\"ds-sheet__head\"><span class=\"ds-sheet__label\">Scheme list</span><span class=\"ds-sheet__count\">3 plants</span></div>\n  <svg class=\"ds-sheet__elevation\" viewBox=\"0 0 280 88\" aria-hidden=\"true\">\n    <path class=\"ds-sheet__ground\" d=\"M8 78 H272\"/>\n    <g class=\"ds-sheet__plant\"><path pathLength=\"1\" d=\"M64 78 C 63 58 61 40 54 26\"/><path pathLength=\"1\" d=\"M64 78 C 64 56 63 36 62 22\"/><path pathLength=\"1\" d=\"M64 78 C 65 58 67 40 73 28\"/><path pathLength=\"1\" d=\"M64 78 C 66 60 69 46 78 34\"/></g>\n    <g class=\"ds-sheet__plant\" style=\"--_delay: 0.09s\"><path pathLength=\"1\" d=\"M134 78 C 136 62 142 50 150 50 C 158 50 164 62 166 78\"/><path pathLength=\"1\" d=\"M141 78 C 142 68 146 60 149 59\"/><path pathLength=\"1\" d=\"M159 78 C 158 68 154 61 151 60\"/></g>\n    <g class=\"ds-sheet__plant\" style=\"--_delay: 0.18s\"><path pathLength=\"1\" d=\"M200 78 C 200 70 200 62 200 56\"/><path pathLength=\"1\" d=\"M200 56 L 192 48\"/><path pathLength=\"1\" d=\"M200 56 L 197 46\"/><path pathLength=\"1\" d=\"M200 56 L 203 46\"/><path pathLength=\"1\" d=\"M200 56 L 208 48\"/><circle cx=\"192\" cy=\"46.5\" r=\"1.4\"/><circle cx=\"197\" cy=\"44.5\" r=\"1.4\"/><circle cx=\"203\" cy=\"44.5\" r=\"1.4\"/><circle cx=\"208\" cy=\"46.5\" r=\"1.4\"/></g>\n    <g class=\"ds-sheet__ghost\"><path pathLength=\"1\" d=\"M220 78 C 223 71 227 71 230 78 C 233 71 237 71 240 78 C 243 72 247 72 250 78\"/></g>\n  </svg>\n  <div class=\"ds-sheet__year\"><span class=\"ds-sheet__month\">J</span><span class=\"ds-sheet__month\">F</span><span class=\"ds-sheet__month\">M</span><span class=\"ds-sheet__month\">A</span><span class=\"ds-sheet__month ds-sheet__month--spring\">M</span><span class=\"ds-sheet__month ds-sheet__month--early-summer\">J</span><span class=\"ds-sheet__month ds-sheet__month--summer\">J</span><span class=\"ds-sheet__month ds-sheet__month--summer ds-sheet__month--now\">A</span><span class=\"ds-sheet__month ds-sheet__month--autumn\">S</span><span class=\"ds-sheet__month ds-sheet__month--autumn\">O</span><span class=\"ds-sheet__month\">N</span><span class=\"ds-sheet__month\">D</span></div>\n</div>",
      "css": ".ds-sheet { --_accent: var(--sem-flowering-spring-fg); --_accent-wash: var(--sem-flowering-spring-bg); --_figure-measure: 22rem; inline-size: 26rem; background: var(--color-white); border: 1px solid var(--sem-border-color); border-radius: var(--radius-m, 4px); overflow: hidden; }\n.ds-sheet__head { display: flex; align-items: baseline; justify-content: space-between; gap: 1rem; padding: 0.5rem 1rem; background: color-mix(in srgb, var(--_accent-wash) 60%, var(--color-white)); border-block-end: 1px solid color-mix(in srgb, var(--_accent) 20%, var(--sem-border-color)); }\n.ds-sheet__label, .ds-sheet__count { font-family: var(--font-mono), ui-monospace, monospace; font-size: 0.8rem; font-weight: 500; letter-spacing: 0.14em; text-transform: uppercase; }\n.ds-sheet__label { color: var(--_accent); }\n.ds-sheet__count { color: color-mix(in srgb, var(--_accent) 78%, transparent); }\n.ds-sheet__elevation { display: block; inline-size: 100%; max-inline-size: var(--_figure-measure); margin-inline: auto; block-size: auto; padding: 0.5rem 1rem 0; box-sizing: border-box; }\n.ds-sheet__ground { fill: none; stroke: color-mix(in srgb, var(--_accent) 40%, var(--sem-border-color)); stroke-width: 1; stroke-linecap: round; }\n.ds-sheet__plant path { fill: none; stroke: var(--_accent); stroke-width: 1.4; stroke-linecap: round; stroke-linejoin: round; stroke-dasharray: 1; stroke-dashoffset: 0; animation: ds-sheet-draw 0.8s cubic-bezier(0.16, 1, 0.3, 1) backwards; animation-delay: var(--_delay, 0s); }\n.ds-sheet__plant circle { fill: var(--_accent); animation: ds-sheet-dot 0.4s ease backwards; animation-delay: calc(var(--_delay, 0s) + 0.4s); }\n.ds-sheet__ghost path { fill: none; stroke: color-mix(in srgb, var(--_accent) 45%, transparent); stroke-width: 1.2; stroke-linecap: round; stroke-dasharray: 0.035 0.045; }\n.ds-sheet__year { display: flex; gap: 4px; max-inline-size: var(--_figure-measure); margin-inline: auto; padding: 4px 1rem 0.5rem; }\n.ds-sheet__month { position: relative; flex: 1; min-inline-size: 0; padding-block: 0.25rem; text-align: center; font-family: var(--font-inter), sans-serif; font-size: 0.73rem; color: var(--color-ink-soft); background: var(--color-paper-deep); border-radius: 2px; }\n.ds-sheet__month--spring { color: var(--sem-flowering-spring-fg); background: var(--sem-flowering-spring-bg); }\n.ds-sheet__month--early-summer { color: var(--sem-flowering-early-summer-fg); background: var(--sem-flowering-early-summer-bg); }\n.ds-sheet__month--summer { color: var(--sem-flowering-summer-fg); background: var(--sem-flowering-summer-bg); }\n.ds-sheet__month--autumn { color: var(--sem-flowering-autumn-fg); background: var(--sem-flowering-autumn-bg); }\n.ds-sheet__month--now::after { content: \"\"; position: absolute; inset-inline: 35%; inset-block-end: 0; block-size: 2px; border-radius: 2px; background: var(--_accent); }\n@keyframes ds-sheet-draw { from { stroke-dashoffset: 1; } to { stroke-dashoffset: 0; } }\n@keyframes ds-sheet-dot { from { opacity: 0; } to { opacity: 1; } }\n@media (prefers-reduced-motion: reduce) { .ds-sheet__plant path, .ds-sheet__plant circle { animation: none; } }"
    },
    {
      "name": "Scheme Generate Action",
      "kind": "custom",
      "refersTo": "button-scheme",
      "description": "The scheme-list pane's finishing action (SchemeGenerateAction.tsx, added alongside the /plant-scheme/chat workspace) — a plain-language line describing what \"Generate the scheme\" will do, pinned beneath the growing plant list on wide screens the same way the chat composer pins beneath its log. Completing swaps the button for a quiet confirmation in the path's own flowering-season accent, never a second button.",
      "html": "<div class=\"ds-generate\"><p class=\"ds-generate__lead\">Happy with your border? We'll save these 6 plants and put together your planting guide — how to arrange them, and how to care for them through the year.</p><button class=\"ds-generate__button\"><svg width=\"16\" height=\"16\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" aria-hidden=\"true\"><path d=\"M14 9.536V7a4 4 0 0 1 4-4h1.5a.5.5 0 0 1 .5.5V5a4 4 0 0 1-4 4 4 4 0 0 0-4 4c0 2 1 3 1 5a5 5 0 0 1-1 3\"/><path d=\"M4 9a5 5 0 0 1 8 4 5 5 0 0 1-8-4\"/><path d=\"M5 21h14\"/></svg>Generate the scheme</button></div>",
      "css": ".ds-generate { display: flex; flex-direction: column; gap: 8px; padding: 16px; max-inline-size: 22rem; border-block-start: 1px solid var(--sem-border-color); } .ds-generate__lead { margin: 0; color: var(--color-ink-soft); font-family: var(--font-inter), sans-serif; font-size: 0.81rem; line-height: 1.5; } .ds-generate__button { display: inline-flex; align-items: center; justify-content: center; gap: 0.5rem; inline-size: 100%; padding: 0.75rem 1rem; border: none; border-radius: var(--radius-s, 2px); font-family: var(--font-inter), sans-serif; font-size: 0.875rem; line-height: 1; cursor: pointer; background-color: var(--color-p-lavender); color: var(--color-p-white); transition: background-color 150ms, color 150ms; } .ds-generate__button:hover { background-color: var(--color-o-vermillion); color: var(--color-n-deep-grey); }"
    }
  ],
  "narrative": {
    "northStar": "The Living Catalogue",
    "overview": "Plotted takes the rigour of a printed nursery plant catalogue — plate numbers, italic botanical Latin, hairline rules, warm paper stock, a faint printed grain — and pulls it forward into something brighter and unmistakably software. It is a knowingly AI-assisted gardening companion, so the surface is allowed to feel technical and tactile at the same time: real texture and warmth, but crisp edges and no nostalgic cosplay. The Nursery Catalogue is a reference for precision and calm, not for period styling.\n\nColour is drawn from the garden itself and used through the interface to echo that richness — sun needs, flowering seasons, plant traits all carry their own hue — but it is held in restraint so it never competes with the gardener's own plant photographs, which are always the brightest thing on the page. Chrome stays quiet; content brings the colour. The single brand accent, a warm marigold, is spent sparingly on primary action and active navigation.\n\nThe atmosphere to protect is alive, bright, warm, friendly, supportive; simple, informative, and never chaotic or clinical. Depth is achieved with tonal paper layers and 1px hairlines rather than shadow — surfaces are flat at rest, and a shadow only ever appears as a response to interaction.",
    "keyCharacteristics": [
      "Warm paper ground (#FAF6EC) with a faint multiply-blended noise grain — never flat white; white is reserved for raised cards.",
      "Fraunces for every display and heading level — bold roman (600) in the product UI, italic 400 on marketing/editorial surfaces; Inter for UI text; Spline Sans Mono for labels, plate numbers and botanical metadata.",
      "Structure carried by 1px hairline borders and paper/sand tonal layering; flat by default.",
      "One brand accent (marigold) used on ≤10% of a screen; a broad --sem-* semantic-colour system carries all trait, sun, season and status colour.",
      "Print-catalogue devices retained: specimen/plate numbers, italic Latin, caption lines, framed image \"plates\".",
      "Two type-adjacent registers: a wide editorial marketing surface (~1120px) and a narrow product measure (~800px)."
    ],
    "rules": [
      {
        "name": "The One Accent Rule",
        "body": "**The One Accent Rule.** Marigold is the only brand accent in product chrome and appears on ≤10% of any screen — primary action and active nav. Anywhere the interface is more colourful than that, the colour is coming from plant *content* (photos, traits, seasons) via the semantic layer, not from chrome.",
        "section": "colors"
      },
      {
        "name": "The Semantic Layer Rule",
        "body": "**The Semantic Layer Rule.** Trait, sun, season and status colours are declared as `--sem-*` tokens that reference the palette (or a purpose-made hex where the review demanded one). Components read `--sem-*` through object-private `--_*` custom properties — never raw `--color-*` — for any stateful colour. New state colours go through this layer.",
        "section": "colors"
      },
      {
        "name": "The Focus Colour Rule",
        "body": "**The Focus Colour Rule.** Every focus ring and every active-field edge in the app reads `--sem-focus-color` — never `--color-b-cyan`, never a per-component value. It resolves to Cyan Deep because a focus indicator is a non-text UI component under WCAG 1.4.11 and must clear 3:1 against whatever it sits on; the app's grounds run from paper to highlight-yellow, so the token is chosen against the lightest of them. A new interactive object gets its focus state from this token, and if the indicator ever needs to change, it changes here once.",
        "section": "colors"
      },
      {
        "name": "The Highlight-Yellow convention",
        "body": "**The Highlight-Yellow convention** *(established pattern, not an invariant).* `highlight-yellow` is the one reactive colour that marks whatever the pointer is on. Keep every new interactive object consistent with it on hover/active. It marks the pointer and **nothing else** — see the next rule.",
        "section": "colors"
      },
      {
        "name": "The Chosen-Is-Filled Rule",
        "body": "**The Chosen-Is-Filled Rule.** In a choose-one group, hover and selected must never share a treatment. Hover takes the highlight-yellow tint; *chosen* takes a **fill** in the context's accent (lavender in Planting Schemes) with its paired light text. The failure this prevents is specific and was live in `.c-scheme-prefs__choice`: hover and `.is-selected` shared one declaration block, so moving the pointer across the group made every option you touched look like the one you had picked. A fill also survives sitting next to a hovered sibling, which a border-colour swap does not. Where the option is a **photo card**, a fill would bury the photograph, so the job passes to a real radio mark that is drawn in *both* states (see Radio card) and hover falls back to the card lift. Selection must additionally be exposed to assistive tech — `aria-pressed` on a button group, or real radio semantics.",
        "section": "colors"
      },
      {
        "name": "The Semantic Field exception",
        "body": "**The Semantic Field exception** *(scoped to the `/plant-scheme` journey).* The flowering-season hues normally appear only in small badges and roundels — \"held in restraint\" per the Overview. The planting-scheme flow is the one sanctioned place a `--sem-flowering-*` pair carries **large surfaces and a whole path's accent**:",
        "section": "colors"
      },
      {
        "name": "The Two-Register Rule",
        "body": "**The Two-Register Rule.** Fraunces *headings* are **bold roman (600)** in the product UI and **italic (400)** on marketing/editorial surfaces — never mixed within one surface. An app section header in italic, or a marketing headline in bold roman, is drift. Weight and style are chosen by surface mode (Operate → roman 600; Persuade/Read editorial → italic 400), not by taste per screen.",
        "section": "typography"
      },
      {
        "name": "The Mono Label Rule",
        "body": "**The Mono Label Rule.** Eyebrows, specimen/plate numbers, field labels and botanical metadata all use `.o-type-label` — Spline Sans Mono, uppercase, `letter-spacing: 0.14em`, weight 500, `.minion` size. One class, no per-context variants; it is the type element that carries the \"tech-aware\" half of the identity.",
        "section": "typography"
      },
      {
        "name": "The One Scale Rule",
        "body": "**The One Scale Rule.** Type size comes only from the printers'-names step classes (`minion → canon` + `long-` variants) in `styles/base/_typography.scss`, and line-height only from `.o-type-leading--*`. Tailwind `text-*` / `font-*` / `leading-*` and arbitrary `text-[Npx]` are not part of this system — they are being converted out. Never introduce a new size value; map the need onto the nearest step.",
        "section": "typography"
      },
      {
        "name": "The Latin-in-Italic Rule",
        "body": "**The Latin-in-Italic Rule.** Botanical names (genus, species, cultivar) are always rendered italic, whatever the surrounding type.",
        "section": "typography"
      },
      {
        "name": "The breakout device",
        "body": "**The breakout device** — `.c-garden-gallery__grid` (the planting-scheme start panel's plant grid) only: a single element, not a page or container measure. `margin-inline: calc(50% - 50vw)` escapes to the viewport edge, then `padding-inline: max(var(--space-md), calc(50vw - 38rem))` re-centres it at a `76rem` cap — wider than any page measure above, but scoped to one grid, not the page around it: the search field and \"In your garden\" heading immediately above it stay at the product measure, so the page visibly opens up right at the grid rather than the whole page widening. Below roughly `60rem` the formula floors back to the page's own `--space-md` gutter, so a phone sees no breakout at all. The same margin-trick the hub's retired first-run teaser used to use for its photo triptych — same mechanism, now spent on the real task (browsing a whole garden) instead of a marketing moment. See Planting schemes hub → Garden gallery.",
        "section": "layout"
      },
      {
        "name": "The Narrow Column Rule",
        "body": "**The Narrow Column Rule.** App pages hold to the ~800px `.o-page` measure — one readable column, one task. The 1120px editorial container is a marketing device and never appears in the app. The **sanctioned width exception** is the dashboard, which runs the ~1000px companion measure (still one centred column, never multi-column) because it is a stack of peer check-in blocks rather than a single task. The `/plant-scheme/chat` workspace (below) is the other. The planting schemes hub (`/plant-scheme`) is no longer a third at the *page* level — it held a bounded two-column exception until a revision retired it in favour of a single column with a \"Recent plans\" preview, so full browsing/management could move to its own route (`/plant-scheme/plans`) instead of sharing the hub with the starting task. Its garden gallery grid does still break out, but as the breakout device (above) — one element opting itself wider, not the page or a container claiming a new measure — which is why it isn't a fourth page-level exception here. A new app route wanting extra width, at the page or container level, is drift, not a precedent; a single element with a real reason to bloom past its container is a different, and much cheaper, decision.",
        "section": "layout"
      },
      {
        "name": "The Flat-By-Default Rule",
        "body": "**The Flat-By-Default Rule.** Surfaces are flat at rest. A shadow appears only as a reaction to state — hover, open, overlay — and never as ambient decoration. If a resting element needs to stand out, reach for a tonal layer or a hairline first.",
        "section": "elevation"
      },
      {
        "name": "The Grain Rule",
        "body": "**The Grain Rule.** The page ground always carries its `feTurbulence` noise texture at ~8% alpha, `multiply` blended. A pure flat `#FFF` or un-grained `#FAF6EC` fill at page level is a regression.",
        "section": "elevation"
      },
      {
        "name": "The One Authored Moment Rule",
        "body": "**The One Authored Moment Rule.** A surface gets one motion that is designed — orchestrated, on the emphasis curve, from an already-visible resting state. Everything else is incidental (default curve) or still. Every keyframe and transition honours `prefers-reduced-motion: reduce` — the authored moment degrades to an instant state change, never a jump.",
        "section": "motion"
      }
    ],
    "dos": [
      "**Do** keep the page ground `#FAF6EC` *with* its noise grain (multiply, ~8%). White is for raised cards, dialogs and menus — and for the chat composer, the one field on a tinted surface that has to read as an unwritten page.",
      "**Do** set product-UI headings in Fraunces bold roman (`.kirk` / 600); use Fraunces italic 400 only on marketing/editorial surfaces (the Two-Register Rule).",
      "**Do** use Spline Sans Mono, uppercase, `0.12–0.16em` tracking for eyebrows, plate numbers, field labels and botanical meta.",
      "**Do** render botanical Latin italic, always — including inside a bold-roman title, where it reads as bold-roman with an italic species.",
      "**Do** carry structure with 1px hairlines (`--sem-border-color`) and tonal paper layers; add shadow only on hover / open / overlay.",
      "**Do** route every stateful colour through a `--sem-*` token and an object-private `--_*` property — never a raw `--color-*` in a modifier.",
      "**Do** keep marigold rare: primary action and active nav, ≤10% of a screen.",
      "**Do** let colour enter through plant content — photos and the trait/sun/season badge families — while chrome stays calm.",
      "**Do** treat the planting schemes hub (`/plant-scheme`) as a single-task page — start a scheme, with a light Recent plans glance above it. Its season-washed start panel belongs to the scheme journey; on another Operate screen it is drift. Full plan browsing and management belongs on `/plant-scheme/plans`, not folded back into the hub. \"How it works\" content belongs in the welcome dialog's `OnboardingCarousel`, shown once to a genuinely first-time gardener — not a standing disclosure on the hub.",
      "**Do** carry the scheme's flowering-season accent through the conversation via `--_accent` / `--_accent-wash`, and reuse the journey chrome (`.c-scheme-journey` step marker, the border elevation, the tray pill) rather than reinventing it per screen.",
      "**Do** hold app pages to the ~800px `.o-page` measure; the 1120px container is a marketing device, the ~1000px companion measure is the dashboard's alone, and full width is the `/plant-scheme/chat` workspace's alone — the Layout section lists every exception. The planting schemes hub sits at the ordinary product measure now too.",
      "**Do** build any new conversation on the `c-chat` primitives (`_chat.scss` / `ChatLog.tsx`) — a boxless internally-scrolling log with `role=\"log\"` + `aria-live` (height capped via `--short` / `--bounded` or flexed to fill; `--bounded` foot-fades toward the composer), attribution once per run, a typing indicator before a reply, an auto-growing composer set a rhythm step off the log, focus back to the composer after send.",
      "**Do** build a choose-one set as a *set* — full-width rows stacked in one column, label leading, a mark drawn in both states held to the trailing edge — and close it once the choice is made, so scrollback reads as a record rather than as live buttons (see Chat → Choose-one options).",
      "**Do** take every focus ring and active-field edge from `--sem-focus-color` (Cyan Deep) — the light `--color-b-cyan` is a fill/selected tint and is too pale to be an indicator.",
      "**Do** honour `prefers-reduced-motion` for every animation (all current keyframes already opt out).",
      "**Do** give every async chat turn a real retry/discard path (`SendFailedNotice` in `ChatLog.tsx`) rather than letting a failure be silent — at most one turn in flight or failed at a time, composer/rows lock until it resolves, exactly two ways out.",
      "**Do** hold every decision point in the chat to ≤4 visible options — split a longer suggestion panel into tier groups (`SuggestionPanel.tsx`) rather than presenting more at once.",
      "**Do** give a closed choose-one group a way back (`.c-chat__options-reopen`) once it's genuinely committed — a permanent choice with no undo is a dead end, not a decision.",
      "**Do** give each pane of a multi-pane layout a real landmark (`<section aria-label>` / `aria-labelledby`) rather than trying to make DOM tab order track pixel position — that only works for one column, and reordering or focus-trapping to chase it is worse than the mismatch itself.",
      "**Do** pair that landmark with a `.u-skip-link` for keyboard-only users with no screen reader — landmarks are AT-specific and don't help them; a visually-hidden-until-focused `<a href=\"#…\">` to a `tabIndex={-1}` section is the equivalent affordance."
    ],
    "donts": [
      "**Don't** introduce a third palette or bring the marketing `moss` / `terra` / `sage` greens into product UI — `styles/abstracts/_variables.scss` is the single source of truth.",
      "**Don't** use `--color-ink` for body copy — running text is `--color-n-deep-grey`; `ink` / `ink-soft` are for headings and labels.",
      "**Don't** drift toward the plant-ID app look: no dark UI, neon accents, camera-first chrome, or gamified score badges. The \"N/12 spotted\" hook is a gentle prompt, not a scoreboard.",
      "**Don't** drift twee: no script or hand-lettered fonts, watercolour blobs, floral borders, or pastel whimsy.",
      "**Don't** set essential copy over a photograph — captions and copy sit beside or below the image (see Shapes → image plate).",
      "**Don't** copy the hub's start-panel washes, the drafts' elevation thumbnails or the annotated example onto another app route — they belong to the scheme journey, not the app at large (see Planting schemes hub).",
      "**Don't** give chat bubbles a marigold (or any accent) fill, or lean on colour alone to tell assistant from user — the split is alignment + fill tone + the attribution line. Keep the season accent on the *wrapper* chrome (`--_accent` on the workspace/list), never inside `c-chat`.",
      "**Don't** mix heading registers within a surface — no italic-400 section header inside the app, no bold-roman headline on a marketing page.",
      "**Don't** add dashboards, KPI tiles, stat walls or streak counters (a PRODUCT.md commitment).",
      "**Don't** put `--color-b-cyan` on a focus ring, a focus outline or an active-field border — that is `--sem-focus-color`'s job (the Focus Colour Rule).",
      "**Don't** let a control's boundary get *weaker* when it reacts. Three controls shipped with a hover that swapped a defined edge for pale cyan or for the hover fill itself, leaving the hovered state with less definition than the resting one. Where the highlight-yellow hover also takes the border, the control must already carry a fill at rest; a transparent or lightly-tinted control keeps its edge and changes only the fill.",
      "**Don't** set a control label in Fraunces. The display face is the heading voice; on a button or an option plate it makes the control read as a card title — as content rather than as something to pick. Inter `--font-weight-medium` is the label voice for controls.",
      "**Don't** use `--color-r-marigold` for error text on paper — it clears only ~3:1, below the 4.5:1 AA floor for body-sized text. Use `--sem-color-error-bg` for inline error copy instead (see the Failed turn entry under Chat).",
      "**Don't** reach for `role=\"radiogroup\"` on options that act on click. Arrow keys in a radiogroup move the selection, which there means firing a request per keypress; a labelled `role=\"group\"` of `aria-pressed` buttons is the honest shape and owes no roving tabindex.",
      "**Don't** build a click target as a `div` with `onClick`. Every one of them in this codebase turned out to be the only path to its action, with no tab stop, no accessible name and nowhere to put a focus state.",
      "**Don't** hardcode radius, spacing or colour values — use the scale tokens; Stylelint checks them.",
      "**Don't** put a resting shadow on a surface to make it \"pop\" — use a tonal step or a hairline.",
      "**Don't** touch `styles/objects/plant-detail.scss` or the `.plant` / `.plant-detail` / `.plant__frost-tolerance` selectors — they're fenced off for a dedicated refactor.",
      "**Don't** add new component CSS as `components/ui/*.module.css` — global SCSS/ITCSS (`styles/objects/`, `styles/components/`) is the settled architecture."
    ]
  }
}