Extend the gold status panel (already shipped on plant detail pages) to
the planting scheme detail page.

CONTEXT
Plant detail pages show a gold panel on first view when fields were
auto-filled by AI lookup: "Some details were filled in automatically —
worth a quick check for accuracy." This ships in prod already, styled
sans-serif with a gold tint background/border, positioned below the
photo. We want the same pattern on the planting scheme detail page,
with a message about the scheme's AI-generated content generally
(not lookup-specific).

PRE-FLIGHT CHECKS (do these before editing, report back what you find)
1. Locate the existing gold panel component from the plant detail page
   and confirm whether it's already a shared/reusable component or
   embedded directly in that page. If embedded, extract it into a
   shared component now rather than duplicating the markup — this is
   the second place it's used, so it should be shared going forward.
2. Locate the `lookup_notice_seen_at`-style first-view tracking used on
   the plant page and confirm how it's scoped (per-plant record). We
   need an equivalent per-scheme flag — check whether the schemes table
   already has anywhere suitable to store this, or whether a column
   needs adding.
3. Confirm the planting scheme detail page's layout structure (hero
   image / title block) so the panel can be positioned correctly.

CHANGES
1. Add a gold panel below the hero image/title block on the planting
   scheme detail page, using the shared/extracted panel component from
   step 1.
2. Message text: something like "This scheme was generated by AI —
   double check plant pairings and timings before relying on it." Feel
   free to propose tighter wording consistent with the app's existing
   voice, but keep the meaning (AI-generated, verify before trusting).
3. Same show-once-then-hide behaviour as the plant page: visible on
   first view of a given scheme, then hidden on subsequent visits. This
   needs its own persisted flag (e.g. `ai_notice_seen_at`) scoped to the
   scheme record — do not reuse or share state with the plant-level
   flag.
4. No error/retry variant needed on this page. If AI generation fails
   for a scheme, that failure is surfaced elsewhere (on the scheme
   landing page, as a separate piece of work) — this page only ever
   renders once a scheme exists, so it only needs the informational
   gold state.

DO NOT
- Touch the plant detail page's existing panel behaviour or markup
  beyond extracting it into a shared component.
- Touch the `lookup_status` field or plant-level AI lookup logic.
- Build the scheme-landing-page error/retry card — that's separate,
  pending a decision on whether original plant selections are
  persisted for retry.
- Change scheme generation logic itself.

EFFORT ESTIMATE
Small–medium — mostly small, but includes extracting a shared component
and adding one new persisted per-scheme flag (likely a migration).
Flag if the migration adds meaningful scope so we can confirm before
you proceed.

DELIVERABLE
Show me the diff before considering this done, including the migration
if one was needed. Flag any assumptions made about wording or column
naming.
