export default async function WeatherSection() {
  return (
    <section aria-label="Weather" className="rounded border border-sand-line p-4 font-sans text-sm text-ink-soft">
      Weather section — placeholder
    </section>
  );
}
